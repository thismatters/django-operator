apiVersion: batch/v1
kind: Job
metadata:
  name: "migrations-{version_slug}"
spec:
  template:
    metadata:
      name: "migrations-{version_slug}"
    spec:
      initContainers: []
      containers:
      - name: finishing
        image: busybox
        command: ["sleep", "1"]
      restartPolicy: Never