apiVersion: v1
kind: Service
metadata:
  name: app-service
spec:
  type: ClusterIP
  ports:
   - protocol: TCP
     port: 8080
     targetPort: {app_port}
  selector:
   role: app
   version: "{version}"
